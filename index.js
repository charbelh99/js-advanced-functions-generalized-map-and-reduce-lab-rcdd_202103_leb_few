function map(array,){
  for(let i=0;i<array.length;i++){
    array[i]=-array[i];
  }
  return array;
}
function map(array,){
  for(let i=0;i<array.length;i++){
    array[i]=array[i]*2;
  }
  return array;
}
function map(array,){
  for(let i=0;i<array.length;i++){
    array[i]=array[i]*array[i];
  }
  return array;
}